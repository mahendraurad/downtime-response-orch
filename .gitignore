# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
node_modules/
frontend/react-app/dist/

# Virtual environments
.venv/
venv/
env/

# Environment variables — never commit
.env

# VS Code
.vscode/settings.json
.vscode/launch.json

# Local agent/editor metadata
.agents/
.codex/

# Test / coverage artifacts
.pytest_cache/
.pytest-tmp/
.coverage
htmlcov/

# Model artifacts (added in Phase 8+)
models/*.pkl
models/*.pt
models/*.bin

# Vector store index (regenerated from SOPs at startup)
data/vector_store/

# Curated signals table — generated by the remediation step (raw stays in git)
data/curated_signals.json
data/trusted_signals.db
data/monitoring_decisions.db
data/execution_results.db
data/hitl_sessions.db
data/hitl_sessions.db-shm
data/hitl_sessions.db-wal
data/learned_cases.json
data/learning_training_rows.jsonl
data/*.db-shm
data/*.db-wal

# Runtime monitoring and orchestration state
data/ewma_state.json
logs/*.log
logs/*.jsonl

# Editor/Office temporary files
~$*
~WRL*.tmp
*.tmp

# Local reference exports that should not be regenerated in commits
Reference/**/*.tmp

# macOS
.DS_Store
